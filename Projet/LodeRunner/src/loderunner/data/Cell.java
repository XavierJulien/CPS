package loderunner.data;

public enum Cell {
	EMP,	// case vide
	PLT,    // case platefome(diggable)
	HOL,  	// case trou
	MTL,   	// case metal(non-diggable)
	PLR, 	// case player
	LAD,    // case ladder
	HDR,    // case handrope
}
