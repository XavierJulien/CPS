package loderunner.data;

public enum Cell {
	EMP,	// case vide
	PLT,    // case platefome(diggable)
	HOL,  	// case trou
	MTL,   	// case metal(non-diggable)
	LAD,    // case ladder
	HDR,    // case handrope
	TLP	    // case téléporteur
}
